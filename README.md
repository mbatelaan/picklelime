# Picklelime

This code will read limefiles and extract the required correlators, then save those as pickle files.

# Installation

On HLRN:

	module load anaconda3
	python -m pip install --editable . --prefix ~/.local/